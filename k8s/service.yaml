apiVersion: v1
kind: Service
metadata:
  name: canvas-mcp
  namespace: canvas-mcp
spec:
  selector:
    app: canvas-mcp
  ports:
    - port: 3001
      targetPort: 3001
      name: http
  type: ClusterIP
---
# Optional: NodePort for direct access without ingress
# apiVersion: v1
# kind: Service
# metadata:
#   name: canvas-mcp-nodeport
#   namespace: canvas-mcp
# spec:
#   selector:
#     app: canvas-mcp
#   ports:
#     - port: 3001
#       targetPort: 3001
#       nodePort: 30001
#   type: NodePort
